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

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

sudo apt systemctl stop jenkins
sudo apt remove jenkins
sudo apt purge jenkins


sudo rm -rf /var/lib/jenkins
sudo rm -rf /etc/jenkins
sudo rm -rf /var/log/jenkins
sudo rm -rf /var/cache/jenkins


git


rm ~/.gitconfig (To Remove)
git config --global user.name "Sagar242004"
git config --global user.mail "dksagar7624910281@gmail.com"






pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Maven Project') {
            steps {
                sh 'mvn clean package'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t pavanwork10/mavenapp:latest .'
            }
        }

        stage('Docker Login') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh 'echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin'
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                sh 'docker push pavanwork10/mavenapp:latest'
            }
        }
    }
}
